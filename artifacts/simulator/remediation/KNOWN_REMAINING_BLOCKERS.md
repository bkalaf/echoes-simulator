# Known remaining blockers

This remediation is not complete and is not represented as complete.

## Canonical blockers

1. The exhaustive V3 research corpus does not exist. The generated queue contains 37,008 field tasks. After 3,679 PET policy nulls and 13 fresh regression results, 33,316 tasks remain unresolved.
2. No canonical V3 research ZIP, zero-`REVIEW_REQUIRED` corpus, full evidence graph, or `ACCEPT_FINAL` adversarial verdict exists.
3. Because research readiness fails, initial Settlement/State resolved-population denominators cannot be proven and the canonical three-world 0–2000 run was not executed.
4. The diagnostic runner still contains aggregate and fixture behavior. The new cohort primitives are tested but are not yet the sole end-to-end runner.
5. Full Breed-level founding waves, yearly migration-intent calculation/traces, form-epoch integration, and the DJT transaction are not integrated into a complete 0–2000 runner. DJT and simultaneous cohort-transfer primitives are implemented and tested in isolation.
6. The desktop workflow still reads prior evidence artifacts for several views. SQLite now has restart-safe checkpoints and atomic naming-barrier persistence, but it is not yet the sole source for every UI query and export.
7. Required Settlement/State detail data, complete export schemas/datasets, and a persisted-run export are not complete.
8. The monorepo and its web tests were intentionally not modified or run because the owner explicitly requires this product to remain standalone.

## Placement conflict

The remediation prompt's `apps/simulator` instruction conflicts with the newer explicit owner instruction and the current SIMULATOR authority. The standalone placement was preserved. No files were written to `/home/bobby/echoes-of-eidolon`.
